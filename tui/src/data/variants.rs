// Placeholder for variant grouping logic
// To be implemented when variant data is available

#[allow(dead_code)]
pub struct VariantGroup {
    pub canonical: String,
    pub variants: Vec<String>,
}
